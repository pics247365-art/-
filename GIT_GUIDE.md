# מדריך Git למתחילים

## מה זה Git?

Git הוא כלי שעוקב אחרי **שינויים בקבצים** לאורך זמן.

תחשוב על זה כמו "שמור גרסה" — רק שבמקום `index_final_v3_REAL.html`, יש לך היסטוריה מסודרת של כל שינוי שעשית, מתי, ולמה.

---

## המושגים הבסיסיים

| מושג | הסבר |
|------|-------|
| **Repository (מאגר)** | התיקייה שמנוהלת על ידי Git — הפרויקט שלך |
| **Commit** | "צילום מסך" של השינויים שלך בנקודת זמן מסוימת |
| **Branch (ענף)** | עותק של הפרויקט שאפשר לעבוד עליו בנפרד |
| **Push** | שליחת השינויים לשרת (GitHub) |
| **Pull** | משיכת שינויים מהשרת אל המחשב שלך |
| **Clone** | הורדת מאגר מהשרת למחשב שלך בפעם הראשונה |

---

## מה יש לך עכשיו בפרויקט הזה?

```
📁 הפרויקט שלך
├── index.html      ← דף הבית של אתר הספרות
├── style.css       ← עיצוב האתר
├── images/         ← תמונות
└── README.md       ← תיאור הפרויקט
```

הפרויקט כבר מנוהל ב-Git עם 4 commits של היסטוריה. אפשר לראות אותם עם:

```bash
git log --oneline
```

---

## הפקודות שתשתמש בהן כל יום

### 1. לראות מה השתנה
```bash
git status
```
מראה אילו קבצים שינית ומה ממתין לשמירה.

### 2. לסמן קבצים לשמירה
```bash
git add index.html        # קובץ ספציפי
git add .                 # כל הקבצים ששונו
```

### 3. לשמור שינויים (commit)
```bash
git commit -m "תיאור קצר של מה שעשית"
```
למשל: `git commit -m "עדכון צבעי הכותרת"`

### 4. לשלוח לGitHub
```bash
git push
```

### 5. לראות היסטוריה
```bash
git log --oneline
```

---

## תרגיל מעשי — נסה עכשיו!

1. פתח את `index.html` ושנה משהו קטן (למשל את שם הספרות)
2. הרץ `git status` — תראה שהקובץ מסומן כ"שונה"
3. הרץ `git add index.html`
4. הרץ `git commit -m "ניסיון ראשון שלי"`
5. הרץ `git log --oneline` — תראה את הcommit החדש שלך!

---

## זרימת עבודה רגילה

```
עורך קוד  →  git add  →  git commit  →  git push
  (עבודה)    (סימון)      (שמירה)       (שליחה)
```

---

## שאלות נפוצות

**ש: שכחתי להוסיף קובץ ל-commit האחרון, מה עושים?**
```bash
git add הקובץ_שנשכח
git commit --amend --no-edit
```

**ש: איך חוזרים לגרסה ישנה?**
```bash
git log --oneline          # מצא את ה-ID של הcommit
git checkout abc1234       # החלף abc1234 ב-ID האמיתי
```

**ש: מה ההבדל בין branch לbranch?**
הענף `main` הוא הגרסה הראשית. ענפים אחרים (כמו `claude/git-basics-guide-U1S0h`) הם מקומות לעבוד על שינויים לפני שמשלבים אותם ל-main.
